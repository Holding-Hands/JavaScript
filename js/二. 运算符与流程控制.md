# 二. 运算符与流程控制

## 1. 赋值运算符与算术运算符

```js
let a = 1; // 赋值语句 a等于1
a => 1

let a = 1 + 2; // 加法
let b = 1 - 2; // 减法
let c = 1 * 2; // 乘法
let d = 2 / 1; // 除法

let a = 1;

1.
a = a + 5;
a += 5; // 上面的简写

2.
a = a * 5;
a *= 5; // 上面的简写

3.
a = a / 5;
a /= 5; // 上面的简写

4.
a = a / 5;
a /= 5; // 上面的简写
```

